import React from 'react'
import { StyledSubmitButton } from './SubmitButton.style'

type Props = {}

const SubmitButton = (props: Props) => {
  return (
    <div>
        <StyledSubmitButton>Regístrate</StyledSubmitButton>
    </div>
  )
}

export default SubmitButton